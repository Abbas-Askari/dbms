import React from "react";

type Props = {
  productId: number;
};

function ProductImageCarousel({ productId }: Props) {
  return <div>ProductImageCarousel</div>;
}

export default ProductImageCarousel;
